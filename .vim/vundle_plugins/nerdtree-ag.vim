" ---------------------------------------------
" NERDTree Ag
" ---------------------------------------------
"
if has('unix') || has('mac')
  if exists('g:vundle_installing_plugins')
    Plugin 'taiansu/nerdtree-ag'
    finish
  endif
endif

